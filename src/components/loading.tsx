/* 
file: loading.tsx 
    component that displays "Loading ..." on screen
    used when data or components are loading it 
*/

import React from 'react'

const Loading = () => {
    return (
        <div>
            Loading ...
        </div>
    )
}

export default Loading